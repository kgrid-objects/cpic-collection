openapi: 3.0.0
info:
  version: 1.0.0
  title: 'CPIC - Genotype to Phenotype for CYP2C19'
  description: 'KGrid CPIC guidelines Genotype to Phenotype for CYP2C19'
servers:
  - url: '/99999/fk4mc97w0h/v0.0.4/'
    description: 'KGrid CPIC guidelines Genotype to Phenotype for CYP2C19'
tags:
  - name: KO Endpoints
    description: KGrid CPIC guidelines Genotype to Phenotype for CYP2C19 Endpoints
paths:
  /phenotype:
    post:
      tags:
       - KO Endpoints
      description: Determine the pheno type based on the genotype.
      operationId: pheno
      requestBody:
        description: inputs
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/genotype'
      responses:
        '200':
          description: response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/phenotype'
        default:
          description: unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
components:
  schemas:
    genotype:
      required:
      - CYP2C19
      properties:
       CYP2C19:
          type: string
          example: '*3/*3'
    phenotype:
      required:
        - CYP2C19
      properties:
        CYP2C19:
          type: object
          required:
            - diplotype
            - phenotype
          properties:
            diplotype:
              type: string
              example: '*3/*3'
            phenotype:
              type: string
              example: 'Likely Poor metabolizer'
    Error:
      required:
        - code
        - message
      properties:
        code:
          type: integer
          format: int32
        message:
          type: string
